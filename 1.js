// jest script

test('test', () => {
    expect(1).toBe(1);
});

test('test', () => {
    expect(1).toBe(1);
});
function add(a, b) {
    return a + b;
}

test('test', () => {
    expect(add(1, 2)).toBe(3);
});